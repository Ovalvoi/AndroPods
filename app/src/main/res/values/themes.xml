<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Compose supplies the real theming. This exists so the manifest has a
        launch theme. values-night/ swaps it to the dark variant so there is no
        light flash before the Compose tree draws.
    -->
    <style name="Theme.AndroPods" parent="@android:style/Theme.Material.Light.NoActionBar" />
</resources>
